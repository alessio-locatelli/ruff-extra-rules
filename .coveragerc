[run]
plugins = covdefaults

[report]
# fail_under defaults to 100 via covdefaults; no override needed now that
# the whole codebase is at 100%.
exclude_also =
    no cover: start(?s:.)*?no cover: stop
