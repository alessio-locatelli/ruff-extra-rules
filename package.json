{
  "private": true,
  "devDependencies": {
    "markdownlint-cli2": "^0.23.2",
    "prettier": "3.9.6"
  },
  "scripts": {
    "format": "prettier --write --cache --log-level warn . && markdownlint-cli2 --fix",
    "format:check": "prettier --check --cache . && markdownlint-cli2"
  }
}
